{% extends '::layout.html.twig' %}

{% block content %}
<div class="col-sm-12">
    <div class="jumbotron">
		<p>{{ 'Current amount of subscribers' | trans }}:{{count}}</p>
		<div class="row">
		    <div class="col-md-12">
		        <a href="{{path('pagePrinterDownload')}}" class="btn btn-primary">
		            {% trans %}Download list subscribers{% endtrans %}</a>
		    </div>
		</div>
	</div>
</div>

{% endblock %}
